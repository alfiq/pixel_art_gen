{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"PixelForge","text":"<p>[!CAUTION] MANDATORY PROTOCOL: All contributors (Human or AI) MUST read docs/ANTI_PATTERNS.md before touching code. Failure to run <code>scripts/verify_repo.py</code> before finalization is a breach of repository integrity and will be caught by CI/CD.</p> <p>A data-free procedural generation engine for high-quality pixel art assets.</p>"},{"location":"#project-overview","title":"Project Overview","text":"<p>This project produces high-quality, symmetrical pixel art (creatures, icons, items) using mathematical rules and artistic heuristics. For a deep dive into the system design, see the Architecture Documentation.</p>"},{"location":"#core-components","title":"Core Components","text":"<ul> <li>Generator: Handles symmetry-masked grid generation.</li> <li>Palette: Dynamic hue-shifting color theory.</li> <li>Renderer: Converts grids to shaded PNGs with outlines.</li> <li>Web Dashboard: A premium FastAPI interface for real-time asset evolution.</li> </ul>"},{"location":"#mandatory-verification-workflow","title":"Mandatory Verification Workflow","text":"<p>To ensure that code, docs, and behavior remain in sync, all developers must run the Verification Pipeline before finalizing changes. <pre><code>uv run scripts/verify_repo.py\n</code></pre></p>"},{"location":"#setup-and-environment","title":"Setup and Environment","text":""},{"location":"#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.12+</li> <li>uv (recommended for dependency management)</li> </ul>"},{"location":"#local-development","title":"Local Development","text":"<ol> <li>Clone the repository.</li> <li>Initialize the environment: <code>uv sync</code>.</li> <li>Start the web server: <code>uv run main.py</code>.</li> <li>Run tests: <code>uv run pytest</code>.</li> </ol>"},{"location":"#coding-standards","title":"Coding Standards","text":"<ul> <li>Type Hinting: Mandatory for all function signatures.</li> <li>Documentation: All files must have Purpose/Dependencies docstrings.</li> <li>Linting: Strict adherence to Ruff standards.</li> </ul>"},{"location":"#enforcement","title":"Enforcement","text":"<p>CAUTION: MANDATORY AGENT PROTOCOL: AI Agents and developers MUST NOT finalize any task or declare a feature complete without a SUCCESSFUL execution of the repository verification script. This script is the ultimate Source of Truth for repository integrity, documentation sync, and behavioral correctness. Failure to run this script before termination is considered a breach of project policy.</p> <ul> <li>Linting: We use ruff (via the verification script) to enforce strict style and documentation standards.</li> <li>Style Rules: All contributions must adhere to the Official Style Guide.</li> <li>Hierarchical Exploration: Documentation must be explored and maintained recursively as defined in docs/ARCHITECTURE.md.</li> </ul>"},{"location":"ANTI_PATTERNS/","title":"PixelForge Anti-Patterns: The Wall of Forbidden Errors","text":"<p>This document serves as a mandatory guide for all contributors (human or autonomous) to prevent the recurrence of critical repository failures discovered during the project's hardening phase.</p>"},{"location":"ANTI_PATTERNS/#1-the-carbon-copy-sync-rule","title":"1. The Carbon-Copy Sync Rule","text":"<p>Mistake: <code>docs/index.md</code> and <code>README.md</code> drifting apart. Enforcement: <code>scripts/verify_repo.py</code> performs a byte-by-byte comparison. Fix: Always copy <code>README.md</code> to <code>docs/index.md</code> after changes.</p>"},{"location":"ANTI_PATTERNS/#2-the-ghost-config-syndrome","title":"2. The Ghost Config Syndrome","text":"<p>Mistake: An empty or missing <code>pyproject.toml</code>. Enforcement: The verification script checks for the existence and basic schema of the project file. Fix: Use <code>uv init</code> or maintain the <code>[project]</code> section with all dependencies properly versioned.</p>"},{"location":"ANTI_PATTERNS/#3-the-shadow-tool-leak","title":"3. The Shadow Tool Leak","text":"<p>Mistake: Typos in <code>.gitignore</code> (e.g., <code>t o o l s /</code>) allowing maintenance scripts into version control. Enforcement: Manual review and automated <code>.gitignore</code> presence check for the <code>tools/</code> entry. Fix: Verify <code>.gitignore</code> syntax after every modification.</p>"},{"location":"ANTI_PATTERNS/#4-the-trust-me-documentation-gap","title":"4. The \"Trust Me\" Documentation Gap","text":"<p>Mistake: Adding functions or methods without <code>Args:</code> and <code>Returns:</code> documentation. Enforcement: Enhanced docstring parser in <code>verify_repo.py</code> and Ruff <code>D100/D101/D102/D103</code> rules. Fix: Every method, including <code>__init__</code>, must document its signature and return type.</p>"},{"location":"ANTI_PATTERNS/#5-non-ascii-character-contamination","title":"5. Non-ASCII Character Contamination","text":"<p>Mistake: Including curly quotes \" \", emojis, or other non-ASCII symbols in text assets. Enforcement: <code>scripts/verify_repo.py</code> ASCII-only validation across the entire tree. Fix: Use standard ASCII characters only. Use tools to strip non-ASCII if needed.</p>"},{"location":"ANTI_PATTERNS/#6-placeholder-navigation","title":"6. Placeholder Navigation","text":"<p>Mistake: Adding files to <code>mkdocs.yml</code> that are empty or contain only boilerplate. Enforcement: Verification script now flags files with 0 or near-zero byte counts that are referenced in documentation. Fix: Fill all documentation files before declaring a task complete.</p> <p>FAILURE TO ADHERE TO THESE GUIDELINES WILL RESULT IN A BLOCKED CI/CD PIPELINE.</p>"},{"location":"ARCHITECTURE/","title":"PixelForge Architectural Model","text":"<p>This document provides a formal representation of the Pixel Art Generator's architecture, data flow, and component relationships.</p>"},{"location":"ARCHITECTURE/#core-architectural-philosophy","title":"Core Architectural Philosophy","text":"<p>The system follows a Modular Pipeline Architecture. Each stage of the generation process is decoupled, allowing for future expansion into new art styles.</p>"},{"location":"ARCHITECTURE/#1-generation-pipeline","title":"1. Generation Pipeline","text":"<p>The internal logic flows from abstract grid generation to concrete image rendering.</p> <pre><code>graph TD\n    A[Random Seed] --&gt; B[SpriteGenerator]\n    B --&gt;|Integer Grid| C[SpriteRenderer]\n    D[PaletteGenerator] --&gt;|Color Ramps| C\n    C --&gt;|PIL Image| E[Output File]\n    E --&gt; F[Dashboard Display]</code></pre>"},{"location":"ARCHITECTURE/#component-roles","title":"Component Roles:","text":"<ul> <li>SpriteGenerator: Uses probabilistic masks and symmetry rules to create a 2D integer matrix representing the DNA of the sprite.</li> <li>PaletteGenerator: Applies color theory heuristics (Hue-Shifting) to create harmonious ramps.</li> <li>SpriteRenderer: The bridge between math and art. It interprets the grid, calculates border lighting, and applies palettes.</li> </ul>"},{"location":"ARCHITECTURE/#2-web-application-flow","title":"2. Web Application Flow","text":"<pre><code>sequenceDiagram\n    participant User as Web Browser\n    participant API as FastAPI Backend\n    participant Core as Generation Engine\n    participant FS as File System\n\n    User-&gt;&gt;API: GET /generate?scale=16\n    API-&gt;&gt;Core: Request new Sprite\n    Core-&gt;&gt;Core: Generate Grid &amp; Palette\n    Core-&gt;&gt;Core: Render PNG\n    Core--&gt;&gt;FS: Save sprite_{uuid}.png\n    API--&gt;&gt;User: JSON {image_url, palette}\n    User-&gt;&gt;User: Update History &amp; Main Preview</code></pre>"},{"location":"ARCHITECTURE/#3-data-schema","title":"3. Data Schema","text":"<ul> <li>The DNA (Grid): A np.ndarray of shape (H, W) where values are [0: Empty, 1: Body, 2: Accent, 3: Detail].</li> <li>The Skin (Palette): A List[List[RGB]] containing 3 ramps of 4-5 shades each.</li> <li>The Asset: A pixel-perfect RGBA PNG file with scale applied via Nearest-Neighbor resampling.</li> </ul>"},{"location":"STYLE_GUIDE/","title":"PixelForge Style Guide &amp; Standards","text":"<p>This document defines the absolute standards for code, documentation, and asset management within the PixelForge repository. All contributors (human or agent) must adhere to these rules.</p>"},{"location":"STYLE_GUIDE/#1-the-zero-emoji-policy","title":"1. The Zero-Emoji Policy","text":"<p>Non-ASCII characters, symbols, and emojis are strictly forbidden in all text-based assets including: - Source code comments - Markdown documentation - Commit messages - Configuration files (YAML, TOML)</p> <p>Rationale: To maintain a professional, high-signal environment and ensure maximum compatibility across all terminals and editors.</p>"},{"location":"STYLE_GUIDE/#2-documentation-hierarchy","title":"2. Documentation Hierarchy","text":"<p>Every Python file MUST contain a top-level docstring with the following structure: - Purpose: A concise description of the file's architectural role. - Dependencies: A list of key internal and external libraries used.</p> <p>Every test function MUST document: - Rationale: Why the test exists. - Process: How the test is performed. - Inputs/Expectations: Clear definition of the test contract.</p>"},{"location":"STYLE_GUIDE/#3-coding-standards","title":"3. Coding Standards","text":""},{"location":"STYLE_GUIDE/#type-hinting","title":"Type Hinting","text":"<p>Mandatory strict type hinting for all: - Function signatures (arguments and return types). - Class attributes. - Complex local variables.</p>"},{"location":"STYLE_GUIDE/#linter-compliance","title":"Linter Compliance","text":"<p>No work is considered complete if <code>ruff check .</code> returns errors.</p>"},{"location":"STYLE_GUIDE/#4-integrity-sync","title":"4. Integrity Sync","text":"<p>No architectural or feature change is finalized without running the Verification Pipeline: <pre><code>uv run scripts/verify_repo.py\n</code></pre> This script ensures that the <code>ARCHITECTURE.md</code>, <code>README.md</code>, and code behaviors are byte-perfect in their alignment. It also enforces the Zero-Emoji Policy, Docstring Standards, and Config Integrity.</p>"},{"location":"STYLE_GUIDE/#5-forbidden-anti-patterns","title":"5. Forbidden Anti-Patterns","text":"<p>Contributors must review ANTI_PATTERNS.md to understand the common failures that block the CI/CD pipeline. Ignorance of these rules is not an excuse for deployment failures.</p>"},{"location":"STYLE_GUIDE/#6-excluded-utilities","title":"6. Excluded Utilities","text":"<p>One-off tools, \"quick-fix\" scripts, or maintenance utilities (like emoji strippers) must reside in the <code>/tools/</code> directory, which is excluded from Git via <code>.gitignore</code>. Core library logic must never depend on files in <code>/tools/</code>.</p>"},{"location":"VERIFICATION_PIPELINE/","title":"Repository Verification Pipeline","text":"<p>To maintain the integrity of the PixelForge Generator, every change MUST be followed by a verification pass. This ensures that the documentation remains a faithful representation of the code structure and behavior.</p>"},{"location":"VERIFICATION_PIPELINE/#mandatory-workflow","title":"Mandatory Workflow","text":"<p>Before finalizing any feature or bugfix, the following verification pipeline must be executed:</p>"},{"location":"VERIFICATION_PIPELINE/#1-automated-verification","title":"1. Automated Verification","text":"<p>Run the following command from the root directory: <pre><code>uv run scripts/verify_repo.py\n</code></pre></p> <p>This script automatically validates: - Linting: Code quality and style (Ruff). - Behavior: Functional correctness (Pytest suite). - Hierarchy: Presence of mandatory docstrings (Purpose/Dependencies). - Sync: Link integrity between README.md and docs/.</p>"},{"location":"VERIFICATION_PIPELINE/#2-manual-verification-artifacts-ui","title":"2. Manual Verification (Artifacts &amp; UI)","text":"<p>If the change affects the User Interface or the Art Algorithm: 1. Start the web server: <code>uv run main.py</code>. 2. Generate at least 5 sprites to check for visual artifacts.</p>"},{"location":"VERIFICATION_PIPELINE/#failure-policy","title":"Failure Policy","text":"<p>If any step in the pipeline fails, the repository is considered out-of-sync. No changes should be finalized until the verification script returns a SUCCESSFUL status.</p>"},{"location":"VERIFICATION_PIPELINE/#agent-mandatory-protocol-amp-01","title":"Agent Mandatory Protocol (AMP-01)","text":"<p>To ensure long-term maintenance by AI assistants, the following protocol is enforced:</p> <ol> <li>Run Pipeline: Before reporting completion to the USER, the agent MUST execute scripts/verify_repo.py.</li> <li>Self-Correction: If the script returns any errors, the agent MUST resolve them and re-run the script until it succeeds.</li> <li>Audit Trail: The output of the verification script should be included in the agent's final progress summary to provide proof of integrity.</li> </ol>"},{"location":"api/generator/","title":"Generator API","text":""},{"location":"api/generator/#pixel_art_gen.generator","title":"<code>pixel_art_gen.generator</code>","text":"<p>Symmetrical grid generation for pixel art assets.</p> Purpose <p>Provide the core mathematical framework for creating symmetrical sprite grids without training data.</p> Dependencies <ul> <li>numpy</li> <li>random</li> </ul>"},{"location":"api/generator/#pixel_art_gen.generator.SpriteGenerator","title":"<code>SpriteGenerator</code>","text":"<p>Generates symmetrical pixel art grids using masks and growth rules.</p> Source code in <code>src/pixel_art_gen/generator.py</code> <pre><code>class SpriteGenerator:\n    \"\"\"Generates symmetrical pixel art grids using masks and growth rules.\"\"\"\n\n    def __init__(self, width: int = 16, height: int = 16) -&gt; None:\n        \"\"\"Initializes the generator with grid dimensions.\n\n        Args:\n            width: Total width of the sprite grid.\n            height: Total height of the sprite grid.\n        \"\"\"\n        self.width = width\n        self.height = height\n\n    def generate(self, seed: int = None) -&gt; np.ndarray:\n        \"\"\"Generates a symmetrical pixel art grid using masks and growth rules.\n\n        Args:\n            seed: Optional random seed for reproducible generation.\n\n        Returns:\n            A 2D numpy array of integers representing the sprite DNA.\n        \"\"\"\n        if seed is not None:\n            # Normalize seat to uint32 for numpy\n            np.random.seed(seed % (2**32))\n            random.seed(seed)\n\n        grid = np.zeros((self.height, self.width), dtype=int)\n        mask_width = (self.width + 1) // 2\n\n        # Core body mask (Symmetry half)\n        mask = np.random.choice([0, 1], size=(self.height, mask_width), p=[0.4, 0.6])\n\n        # Apply cellular growth to smooth it out\n        for _ in range(2):\n            new_mask = mask.copy()\n            for y in range(1, self.height - 1):\n                for x in range(1, mask_width - 1):\n                    neighbors = np.sum(mask[y-1:y+2, x-1:x+2]) - mask[y, x]\n                    if mask[y, x] == 1 and neighbors &lt; 2:\n                        new_mask[y, x] = 0\n                    elif mask[y, x] == 0 and neighbors &gt; 4:\n                        new_mask[y, x] = 1\n            mask = new_mask\n\n        # Assign shades (1: Body, 2: Accent, 3: Highlight)\n        for y in range(self.height):\n            for x in range(mask_width):\n                if mask[y, x] == 1:\n                    grid[y, x] = np.random.choice([1, 2, 3], p=[0.7, 0.2, 0.1])\n\n        # Mirror the grid\n        for y in range(self.height):\n            for x in range(mask_width, self.width):\n                grid[y, x] = grid[y, self.width - 1 - x]\n\n        return grid\n</code></pre>"},{"location":"api/generator/#pixel_art_gen.generator.SpriteGenerator.__init__","title":"<code>__init__(width=16, height=16)</code>","text":"<p>Initializes the generator with grid dimensions.</p> <p>Parameters:</p> Name Type Description Default <code>width</code> <code>int</code> <p>Total width of the sprite grid.</p> <code>16</code> <code>height</code> <code>int</code> <p>Total height of the sprite grid.</p> <code>16</code> Source code in <code>src/pixel_art_gen/generator.py</code> <pre><code>def __init__(self, width: int = 16, height: int = 16) -&gt; None:\n    \"\"\"Initializes the generator with grid dimensions.\n\n    Args:\n        width: Total width of the sprite grid.\n        height: Total height of the sprite grid.\n    \"\"\"\n    self.width = width\n    self.height = height\n</code></pre>"},{"location":"api/generator/#pixel_art_gen.generator.SpriteGenerator.generate","title":"<code>generate(seed=None)</code>","text":"<p>Generates a symmetrical pixel art grid using masks and growth rules.</p> <p>Parameters:</p> Name Type Description Default <code>seed</code> <code>int</code> <p>Optional random seed for reproducible generation.</p> <code>None</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>A 2D numpy array of integers representing the sprite DNA.</p> Source code in <code>src/pixel_art_gen/generator.py</code> <pre><code>def generate(self, seed: int = None) -&gt; np.ndarray:\n    \"\"\"Generates a symmetrical pixel art grid using masks and growth rules.\n\n    Args:\n        seed: Optional random seed for reproducible generation.\n\n    Returns:\n        A 2D numpy array of integers representing the sprite DNA.\n    \"\"\"\n    if seed is not None:\n        # Normalize seat to uint32 for numpy\n        np.random.seed(seed % (2**32))\n        random.seed(seed)\n\n    grid = np.zeros((self.height, self.width), dtype=int)\n    mask_width = (self.width + 1) // 2\n\n    # Core body mask (Symmetry half)\n    mask = np.random.choice([0, 1], size=(self.height, mask_width), p=[0.4, 0.6])\n\n    # Apply cellular growth to smooth it out\n    for _ in range(2):\n        new_mask = mask.copy()\n        for y in range(1, self.height - 1):\n            for x in range(1, mask_width - 1):\n                neighbors = np.sum(mask[y-1:y+2, x-1:x+2]) - mask[y, x]\n                if mask[y, x] == 1 and neighbors &lt; 2:\n                    new_mask[y, x] = 0\n                elif mask[y, x] == 0 and neighbors &gt; 4:\n                    new_mask[y, x] = 1\n        mask = new_mask\n\n    # Assign shades (1: Body, 2: Accent, 3: Highlight)\n    for y in range(self.height):\n        for x in range(mask_width):\n            if mask[y, x] == 1:\n                grid[y, x] = np.random.choice([1, 2, 3], p=[0.7, 0.2, 0.1])\n\n    # Mirror the grid\n    for y in range(self.height):\n        for x in range(mask_width, self.width):\n            grid[y, x] = grid[y, self.width - 1 - x]\n\n    return grid\n</code></pre>"},{"location":"api/palette/","title":"Palette API","text":""},{"location":"api/palette/#pixel_art_gen.palette","title":"<code>pixel_art_gen.palette</code>","text":"<p>Procedural color palette generation with hue-shifting.</p> Purpose <p>Provide harmonious color ramps using artistic lighting heuristics.</p> Dependencies <ul> <li>colorsys (standard library)</li> </ul>"},{"location":"api/palette/#pixel_art_gen.palette.PaletteGenerator","title":"<code>PaletteGenerator</code>","text":"<p>Generates harmonious color ramps using hue-shifting logic.</p> Source code in <code>src/pixel_art_gen/palette.py</code> <pre><code>class PaletteGenerator:\n    \"\"\"Generates harmonious color ramps using hue-shifting logic.\"\"\"\n\n    @staticmethod\n    def hue_shift(h: float, amount: float) -&gt; float:\n        \"\"\"Shifts the hue within the 0-1 range.\n\n        Args:\n            h: Current hue (0-1).\n            amount: Amount to shift.\n\n        Returns:\n            Shifted hue.\n        \"\"\"\n        return (h + amount) % 1.0\n\n    @classmethod\n    def generate_ramp(\n        cls, \n        base_color: RGB, \n        steps: int = 4, \n        hue_shift_amount: float = 0.05\n    ) -&gt; List[RGB]:\n        \"\"\"Generates a color ramp from a base color with hue shifting.\n\n        Args:\n            base_color: The starting RGB color.\n            steps: Number of shades to generate.\n            hue_shift_amount: How much to shift hue per step.\n\n        Returns:\n            A list of RGB tuples representing the ramp from darkest to lightest.\n        \"\"\"\n        # Convert RGB to HLS (Hue, Lightness, Saturation)\n        r, g, b = [x / 255.0 for x in base_color]\n        h, l_light, s = colorsys.rgb_to_hls(r, g, b)\n\n        ramp: List[RGB] = []\n\n        for i in range(steps):\n            target_l = 0.15 + (i / (steps - 1)) * 0.75\n            shift = (target_l - 0.5) * hue_shift_amount\n            new_h = cls.hue_shift(h, shift)\n            new_s = max(0.1, min(1.0, s + (0.5 - target_l) * 0.2))\n\n            new_r, new_g, new_b = colorsys.hls_to_rgb(new_h, target_l, new_s)\n            ramp.append((int(new_r * 255), int(new_g * 255), int(new_b * 255)))\n\n        return ramp\n\n    @classmethod\n    def get_random_palette(cls) -&gt; List[List[RGB]]:\n        \"\"\"Generates a random multi-ramp palette (Main, Accent, Detail).\n\n        Returns:\n            List of ramps.\n        \"\"\"\n        import random\n\n        base_h = random.random()\n        accent_h = (base_h + 0.5) % 1.0\n        detail_h = (base_h + 0.3) % 1.0\n\n        palettes = []\n        for val_h in [base_h, accent_h, detail_h]:\n            r_val, g_val, b_val = colorsys.hls_to_rgb(val_h, 0.5, 0.7)\n            rgb_base = (int(r_val * 255), int(g_val * 255), int(b_val * 255))\n            palettes.append(cls.generate_ramp(rgb_base))\n\n        return palettes\n</code></pre>"},{"location":"api/palette/#pixel_art_gen.palette.PaletteGenerator.generate_ramp","title":"<code>generate_ramp(base_color, steps=4, hue_shift_amount=0.05)</code>  <code>classmethod</code>","text":"<p>Generates a color ramp from a base color with hue shifting.</p> <p>Parameters:</p> Name Type Description Default <code>base_color</code> <code>RGB</code> <p>The starting RGB color.</p> required <code>steps</code> <code>int</code> <p>Number of shades to generate.</p> <code>4</code> <code>hue_shift_amount</code> <code>float</code> <p>How much to shift hue per step.</p> <code>0.05</code> <p>Returns:</p> Type Description <code>List[RGB]</code> <p>A list of RGB tuples representing the ramp from darkest to lightest.</p> Source code in <code>src/pixel_art_gen/palette.py</code> <pre><code>@classmethod\ndef generate_ramp(\n    cls, \n    base_color: RGB, \n    steps: int = 4, \n    hue_shift_amount: float = 0.05\n) -&gt; List[RGB]:\n    \"\"\"Generates a color ramp from a base color with hue shifting.\n\n    Args:\n        base_color: The starting RGB color.\n        steps: Number of shades to generate.\n        hue_shift_amount: How much to shift hue per step.\n\n    Returns:\n        A list of RGB tuples representing the ramp from darkest to lightest.\n    \"\"\"\n    # Convert RGB to HLS (Hue, Lightness, Saturation)\n    r, g, b = [x / 255.0 for x in base_color]\n    h, l_light, s = colorsys.rgb_to_hls(r, g, b)\n\n    ramp: List[RGB] = []\n\n    for i in range(steps):\n        target_l = 0.15 + (i / (steps - 1)) * 0.75\n        shift = (target_l - 0.5) * hue_shift_amount\n        new_h = cls.hue_shift(h, shift)\n        new_s = max(0.1, min(1.0, s + (0.5 - target_l) * 0.2))\n\n        new_r, new_g, new_b = colorsys.hls_to_rgb(new_h, target_l, new_s)\n        ramp.append((int(new_r * 255), int(new_g * 255), int(new_b * 255)))\n\n    return ramp\n</code></pre>"},{"location":"api/palette/#pixel_art_gen.palette.PaletteGenerator.get_random_palette","title":"<code>get_random_palette()</code>  <code>classmethod</code>","text":"<p>Generates a random multi-ramp palette (Main, Accent, Detail).</p> <p>Returns:</p> Type Description <code>List[List[RGB]]</code> <p>List of ramps.</p> Source code in <code>src/pixel_art_gen/palette.py</code> <pre><code>@classmethod\ndef get_random_palette(cls) -&gt; List[List[RGB]]:\n    \"\"\"Generates a random multi-ramp palette (Main, Accent, Detail).\n\n    Returns:\n        List of ramps.\n    \"\"\"\n    import random\n\n    base_h = random.random()\n    accent_h = (base_h + 0.5) % 1.0\n    detail_h = (base_h + 0.3) % 1.0\n\n    palettes = []\n    for val_h in [base_h, accent_h, detail_h]:\n        r_val, g_val, b_val = colorsys.hls_to_rgb(val_h, 0.5, 0.7)\n        rgb_base = (int(r_val * 255), int(g_val * 255), int(b_val * 255))\n        palettes.append(cls.generate_ramp(rgb_base))\n\n    return palettes\n</code></pre>"},{"location":"api/palette/#pixel_art_gen.palette.PaletteGenerator.hue_shift","title":"<code>hue_shift(h, amount)</code>  <code>staticmethod</code>","text":"<p>Shifts the hue within the 0-1 range.</p> <p>Parameters:</p> Name Type Description Default <code>h</code> <code>float</code> <p>Current hue (0-1).</p> required <code>amount</code> <code>float</code> <p>Amount to shift.</p> required <p>Returns:</p> Type Description <code>float</code> <p>Shifted hue.</p> Source code in <code>src/pixel_art_gen/palette.py</code> <pre><code>@staticmethod\ndef hue_shift(h: float, amount: float) -&gt; float:\n    \"\"\"Shifts the hue within the 0-1 range.\n\n    Args:\n        h: Current hue (0-1).\n        amount: Amount to shift.\n\n    Returns:\n        Shifted hue.\n    \"\"\"\n    return (h + amount) % 1.0\n</code></pre>"},{"location":"api/renderer/","title":"Renderer API","text":""},{"location":"api/renderer/#pixel_art_gen.renderer","title":"<code>pixel_art_gen.renderer</code>","text":"<p>Sprite rendering logic with outlines and shading.</p> Purpose <p>Convert 2D integer grids into pixel-perfect RGBA PNG images.</p> Dependencies <ul> <li>numpy</li> <li>pillow (PIL)</li> </ul>"},{"location":"api/renderer/#pixel_art_gen.renderer.SpriteRenderer","title":"<code>SpriteRenderer</code>","text":"<p>Renders sprite grids into images with artistic effects.</p> Source code in <code>src/pixel_art_gen/renderer.py</code> <pre><code>class SpriteRenderer:\n    \"\"\"Renders sprite grids into images with artistic effects.\"\"\"\n\n    def __init__(self, scale: int = 8) -&gt; None:\n        \"\"\"Initializes the renderer with a scaling factor.\n\n        Args:\n            scale: Pixel multiplier for the final image output.\n        \"\"\"\n        self.scale = scale\n\n    def render(self, grid: np.ndarray, palette: list) -&gt; Image.Image:\n        \"\"\"Renders the grid into a PIL Image.\n\n        Args:\n            grid: 2D integer array.\n            palette: Primary, Secondary, and Detail ramps.\n\n        Returns:\n            A scaled PIL Image object.\n        \"\"\"\n        h, w = grid.shape\n        # Create an RGBA image\n        img_data = np.zeros((h, w, 4), dtype=np.uint8)\n\n        # Simple mapping:\n        # 0: Transparent\n        # 1: Body (Palette[0])\n        # 2: Accent (Palette[1])\n        # 3: Detail (Palette[2])\n\n        for y in range(h):\n            for x in range(w):\n                val = grid[y, x]\n                if val == 0:\n                    continue\n\n                # Check for \"outline\" (neighbors are 0)\n                is_outline = False\n                for dy, dx in [(-1, 0), (1, 0), (0, -1), (0, 1)]:\n                    ny, nx = y + dy, x + dx\n                    if 0 &lt;= ny &lt; h and 0 &lt;= nx &lt; w:\n                        if grid[ny, nx] == 0:\n                            is_outline = True\n                            break\n                    else:\n                        is_outline = True # Edge of image is outline\n                        break\n\n                if is_outline:\n                    # Black outline\n                    img_data[y, x] = [20, 20, 25, 255]\n                else:\n                    # Map to middle shade of the ramp\n                    ramp = palette[val - 1]\n                    color = ramp[len(ramp)//2]\n                    img_data[y, x] = [*color, 255]\n\n        img = Image.fromarray(img_data, \"RGBA\")\n        return img.resize((w * self.scale, h * self.scale), resample=Image.NEAREST)\n</code></pre>"},{"location":"api/renderer/#pixel_art_gen.renderer.SpriteRenderer.__init__","title":"<code>__init__(scale=8)</code>","text":"<p>Initializes the renderer with a scaling factor.</p> <p>Parameters:</p> Name Type Description Default <code>scale</code> <code>int</code> <p>Pixel multiplier for the final image output.</p> <code>8</code> Source code in <code>src/pixel_art_gen/renderer.py</code> <pre><code>def __init__(self, scale: int = 8) -&gt; None:\n    \"\"\"Initializes the renderer with a scaling factor.\n\n    Args:\n        scale: Pixel multiplier for the final image output.\n    \"\"\"\n    self.scale = scale\n</code></pre>"},{"location":"api/renderer/#pixel_art_gen.renderer.SpriteRenderer.render","title":"<code>render(grid, palette)</code>","text":"<p>Renders the grid into a PIL Image.</p> <p>Parameters:</p> Name Type Description Default <code>grid</code> <code>ndarray</code> <p>2D integer array.</p> required <code>palette</code> <code>list</code> <p>Primary, Secondary, and Detail ramps.</p> required <p>Returns:</p> Type Description <code>Image</code> <p>A scaled PIL Image object.</p> Source code in <code>src/pixel_art_gen/renderer.py</code> <pre><code>def render(self, grid: np.ndarray, palette: list) -&gt; Image.Image:\n    \"\"\"Renders the grid into a PIL Image.\n\n    Args:\n        grid: 2D integer array.\n        palette: Primary, Secondary, and Detail ramps.\n\n    Returns:\n        A scaled PIL Image object.\n    \"\"\"\n    h, w = grid.shape\n    # Create an RGBA image\n    img_data = np.zeros((h, w, 4), dtype=np.uint8)\n\n    # Simple mapping:\n    # 0: Transparent\n    # 1: Body (Palette[0])\n    # 2: Accent (Palette[1])\n    # 3: Detail (Palette[2])\n\n    for y in range(h):\n        for x in range(w):\n            val = grid[y, x]\n            if val == 0:\n                continue\n\n            # Check for \"outline\" (neighbors are 0)\n            is_outline = False\n            for dy, dx in [(-1, 0), (1, 0), (0, -1), (0, 1)]:\n                ny, nx = y + dy, x + dx\n                if 0 &lt;= ny &lt; h and 0 &lt;= nx &lt; w:\n                    if grid[ny, nx] == 0:\n                        is_outline = True\n                        break\n                else:\n                    is_outline = True # Edge of image is outline\n                    break\n\n            if is_outline:\n                # Black outline\n                img_data[y, x] = [20, 20, 25, 255]\n            else:\n                # Map to middle shade of the ramp\n                ramp = palette[val - 1]\n                color = ramp[len(ramp)//2]\n                img_data[y, x] = [*color, 255]\n\n    img = Image.fromarray(img_data, \"RGBA\")\n    return img.resize((w * self.scale, h * self.scale), resample=Image.NEAREST)\n</code></pre>"}]}